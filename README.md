# Telegram_gemini_Bot
The Telegram bot registers users by saving their first_name, username, and chat_id in MongoDB. It uses the Gemini API for AI-powered chat and logs interactions. The bot accepts images/PDFs, analyzes them via Gemini, and stores metadata. Users can perform web searches
